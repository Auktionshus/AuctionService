For Register API:

Vælg "POST" som metoden i rullemenuen til venstre for URL-feltet.
Indtast din API's URL i URL-feltet, f.eks. http://localhost:5065/api/register.
Klik på "Body" under URL-feltet.
Vælg "x-www-form-urlencoded".
Tilføj følgende nøgle-værdi par:
Nøgle: email, Værdi: (din e-mail-adresse)
Nøgle: password, Værdi: (dit ønskede kodeord)
Klik på "Send" for at teste Register API.
For Login API:

Vælg "POST" som metoden i rullemenuen til venstre for URL-feltet.
Indtast din API's URL i URL-feltet, f.eks. http://localhost:5065/api/login.
Klik på "Body" under URL-feltet.
Vælg "x-www-form-urlencoded".
Tilføj følgende nøgle-værdi par:
Nøgle: email, Værdi: (din e-mail-adresse)
Nøgle: password, Værdi: (dit kodeord)
Klik på "Send" for at teste Login API.
For Logout API:

Vælg "POST" som metoden i rullemenuen til venstre for URL-feltet.
Indtast din API's URL i URL-feltet, f.eks. http://localhost:5065/api/logout.
Klik på "Send" for at teste Logout API.